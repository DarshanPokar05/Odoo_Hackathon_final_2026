# DealFlow360 — API Contracts

> **Purpose:** This file is the single source of truth for every HTTP endpoint and Socket.io event in the DealFlow360 platform.
> Both the frontend dev and backend dev must append to this file as each module is implemented.
>
> **Conventions:**
> - All REST endpoints are prefixed `/api/`
> - Authentication: `Authorization: Bearer <jwt>` header unless marked `[PUBLIC]`
> - All request/response bodies are JSON unless noted
> - Timestamps are ISO-8601 strings
> - Paginated list responses follow `{ success: true, data: { items: [], total, page, limit } }`
> - Error responses follow `{ success: false, error: { code, message, details? } }`
> - Success responses follow `{ success: true, data: <payload> }`
> - CUSTOMER-role tokens carry an additional `customerId` claim and are scoped to their own data
>
> **Socket.io** — authenticate by passing `{ auth: { token: "<jwt>" } }` on connect.
> Rooms joined automatically: `user:{userId}`, `role:{role}`, and `customer:{customerId}` (CUSTOMER role only).

---

## Health

| Method | Path      | Auth    | Description             |
|--------|-----------|---------|-------------------------|
| GET    | `/health` | PUBLIC  | Liveness check — returns `{ status: "ok", ts }` |

---

## Auth — `/api/auth`

_Implementation: `src/modules/auth/`_

| Method | Path                       | Auth    | Roles | Description |
|--------|----------------------------|---------|-------|-------------|
| POST   | `/api/auth/signup`         | PUBLIC  | —     | Create internal user (ADMIN, SALES_REP, SALES_MANAGER, FINANCE only). Body: `{ name, email, password, role? }` |
| POST   | `/api/auth/login`          | PUBLIC  | —     | Login. Returns `{ token, user }` or `{ mustChangePassword: true, userId }` for first-time portal customers |
| POST   | `/api/auth/change-password`| PUBLIC  | —     | Complete forced password change. Body: `{ userId, newPassword }`. Returns `{ token, user }` |
| GET    | `/api/auth/me`             | JWT     | ALL   | Return current user profile |

**Signup notes:**
- `role` defaults to `SALES_REP` if omitted
- `CUSTOMER` role is rejected — portal accounts are auto-generated by the customer onboarding flow
- Returns `{ user, token }` on success (201)

**Login notes:**
- If `mustChangePassword = true`, no token is issued; client must call `/change-password` first
- Token payload: `{ userId, email, role, customerId? }`

---

## Customers — `/api/customers`

_Implementation: `src/modules/customers/`_

| Method | Path                  | Auth | Roles                                    | Description |
|--------|-----------------------|------|------------------------------------------|-------------|
| GET    | `/api/customers`      | JWT  | ADMIN, SALES_REP, SALES_MANAGER, FINANCE | List all customers |
| GET    | `/api/customers/:id`  | JWT  | ADMIN, SALES_REP, SALES_MANAGER, FINANCE | Get single customer |
| POST   | `/api/customers`      | JWT  | ADMIN, SALES_MANAGER                     | Create customer |
| PUT    | `/api/customers/:id`  | JWT  | ADMIN, SALES_MANAGER                     | Update customer |
| DELETE | `/api/customers/:id`  | JWT  | ADMIN                                    | Delete customer |

---

## Products — `/api/products`

_Implementation: `src/modules/products/`_

| Method | Path                   | Auth | Roles                                    | Description |
|--------|------------------------|------|------------------------------------------|-------------|
| GET    | `/api/products`        | JWT  | ALL internal                             | List products (supports filtering by category) |
| GET    | `/api/products/:id`    | JWT  | ALL internal                             | Get product with variants |
| POST   | `/api/products`        | JWT  | ADMIN                                    | Create product |
| PUT    | `/api/products/:id`    | JWT  | ADMIN                                    | Update product |
| DELETE | `/api/products/:id`    | JWT  | ADMIN                                    | Delete product |

---

## Categories — `/api/categories`

_Implementation: `src/modules/categories/`_

| Method | Path                     | Auth | Roles         | Description |
|--------|--------------------------|------|---------------|-------------|
| GET    | `/api/categories`        | JWT  | ALL internal  | List categories |
| GET    | `/api/categories/:id`    | JWT  | ALL internal  | Get category |
| POST   | `/api/categories`        | JWT  | ADMIN         | Create category |
| PUT    | `/api/categories/:id`    | JWT  | ADMIN         | Update category (incl. maxDiscountPercent) |
| DELETE | `/api/categories/:id`    | JWT  | ADMIN         | Delete category |

---

## Price Lists — `/api/price-lists`

_Implementation: `src/modules/priceLists/`_

| Method | Path                      | Auth | Roles         | Description |
|--------|---------------------------|------|---------------|-------------|
| GET    | `/api/price-lists`        | JWT  | ALL internal  | List all pricing rules |
| GET    | `/api/price-lists/:id`    | JWT  | ALL internal  | Get single rule |
| POST   | `/api/price-lists`        | JWT  | ADMIN         | Create rule |
| PUT    | `/api/price-lists/:id`    | JWT  | ADMIN         | Update rule |
| DELETE | `/api/price-lists/:id`    | JWT  | ADMIN         | Delete rule |

---

## Discount Config — `/api/discount-config`

_Implementation: `src/modules/discountConfig/`_

| Method | Path                          | Auth | Roles | Description |
|--------|-------------------------------|------|-------|-------------|
| GET    | `/api/discount-config`        | JWT  | ADMIN, SALES_MANAGER, FINANCE | List tier discount ceilings |
| GET    | `/api/discount-config/:id`    | JWT  | ADMIN, SALES_MANAGER, FINANCE | Get single ceiling |
| POST   | `/api/discount-config`        | JWT  | ADMIN | Create ceiling |
| PUT    | `/api/discount-config/:id`    | JWT  | ADMIN | Update ceiling |
| DELETE | `/api/discount-config/:id`    | JWT  | ADMIN | Delete ceiling |

---

## Quotations — `/api/quotations`

_Implementation: `src/modules/quotations/`_

| Method | Path                                     | Auth | Roles                                              | Description |
|--------|------------------------------------------|------|----------------------------------------------------|-------------|
| GET    | `/api/quotations`                        | JWT  | ADMIN, SALES_REP, SALES_MANAGER, FINANCE           | List quotations (filtered by rep for SALES_REP) |
| GET    | `/api/quotations/:id`                    | JWT  | ADMIN, SALES_REP, SALES_MANAGER, FINANCE, CUSTOMER | Get quotation with lines, steps, messages |
| POST   | `/api/quotations`                        | JWT  | ADMIN, SALES_REP                                   | Create draft quotation |
| PUT    | `/api/quotations/:id`                    | JWT  | ADMIN, SALES_REP                                   | Update quotation |
| DELETE | `/api/quotations/:id`                    | JWT  | ADMIN                                              | Delete quotation |
| POST   | `/api/quotations/:id/submit`             | JWT  | ADMIN, SALES_REP                                   | Submit for approval (triggers blended risk score + approval chain) |
| POST   | `/api/quotations/:id/confirm`            | JWT  | ADMIN, SALES_MANAGER                               | Confirm approved quotation → creates Order |
| POST   | `/api/quotations/:id/negotiate`          | JWT  | ADMIN, SALES_REP, CUSTOMER                         | Send negotiation message |

### Socket.io Events

| Event               | Direction      | Payload                                | Description |
|---------------------|----------------|----------------------------------------|-------------|
| `QUOTATION_UPDATED` | Server→Client  | `{ quotationId, status, ...extra }`    | Status change — pushed to rep's room, role:SALES_MANAGER, role:FINANCE |
| `NEGOTIATION_MESSAGE` | Server→Client | `{ quotationId, message, senderType }` | New negotiation message — pushed to rep + customer:{customerId} |

---

## Approvals — `/api/approvals`

_Implementation: `src/modules/approvals/`_

| Method | Path                              | Auth | Roles                              | Description |
|--------|-----------------------------------|------|------------------------------------|-------------|
| GET    | `/api/approvals`                  | JWT  | ADMIN, SALES_MANAGER, FINANCE      | List pending approval steps |
| GET    | `/api/approvals/:id`              | JWT  | ADMIN, SALES_MANAGER, FINANCE      | Get single step |
| POST   | `/api/approvals/:id/approve`      | JWT  | SALES_MANAGER, FINANCE             | Approve a step |
| POST   | `/api/approvals/:id/reject`       | JWT  | SALES_MANAGER, FINANCE             | Reject a step |
| POST   | `/api/approvals/:id/return`       | JWT  | SALES_MANAGER, FINANCE             | Return for revision |

### Socket.io Events

| Event              | Direction     | Payload                                  | Description |
|--------------------|---------------|------------------------------------------|-------------|
| `APPROVAL_PENDING` | Server→Client | `{ quotationId, level }`                 | New step waiting — pushed to role:{level} |
| `APPROVAL_ACTED`   | Server→Client | `{ quotationId, level, status, reason }` | Approver acted — pushed to rep's user room |

---

## Upsell Rules — `/api/upsell`

_Implementation: `src/modules/upsell/`_

| Method | Path                  | Auth | Roles                              | Description |
|--------|-----------------------|------|------------------------------------|-------------|
| GET    | `/api/upsell`         | JWT  | ADMIN, SALES_REP, SALES_MANAGER    | List upsell rules |
| GET    | `/api/upsell/:id`     | JWT  | ADMIN, SALES_REP, SALES_MANAGER    | Get single rule |
| POST   | `/api/upsell`         | JWT  | ADMIN                              | Create rule |
| PUT    | `/api/upsell/:id`     | JWT  | ADMIN                              | Update rule |
| DELETE | `/api/upsell/:id`     | JWT  | ADMIN                              | Delete rule |

---

## Warehouses — `/api/warehouses`

_Implementation: `src/modules/warehouses/`_

> Stock-level sub-routes live under each warehouse's `:id`. All stock operations go through the warehouse owner resource.

| Method | Path                                  | Auth | Roles                                              | Description |
|--------|---------------------------------------|------|----------------------------------------------------|-------------|
| GET    | `/api/warehouses`                     | JWT  | ADMIN, SALES_MANAGER, FINANCE, SALES_REP           | List all warehouses, each with their full `stockLevels` array (includes product name/unit) |
| GET    | `/api/warehouses/:id`                 | JWT  | ADMIN, SALES_MANAGER, FINANCE, SALES_REP           | Get single warehouse with stock levels |
| POST   | `/api/warehouses`                     | JWT  | ADMIN                                              | Create warehouse. Body: `{ name, location?, shippingCostWeight? }` |
| PUT    | `/api/warehouses/:id`                 | JWT  | ADMIN                                              | Update warehouse fields (all optional) |
| DELETE | `/api/warehouses/:id`                 | JWT  | ADMIN                                              | Delete warehouse |
| GET    | `/api/warehouses/:id/stock`           | JWT  | ADMIN, SALES_MANAGER, FINANCE, SALES_REP           | List all StockLevel rows for this warehouse |
| PUT    | `/api/warehouses/:id/stock`           | JWT  | ADMIN, FINANCE                                     | Upsert (set absolute values) a StockLevel. Body: `{ productId, onHand, reserved?, reorderPoint?, reorderQty? }` |
| POST   | `/api/warehouses/:id/stock/adjust`    | JWT  | ADMIN, FINANCE                                     | Apply a signed delta to `onHand`. Body: `{ productId, delta, reason }`. Positive = restock, negative = drawdown. Rejects if result < 0 or < reserved. |

**Stock adjust notes:**
- `delta` is a signed integer — pass `-10` to remove 10 units, `+20` to add 20
- `reason` is required for audit trail
- On any stock change: emits `STOCK_UPDATED` to all internal role rooms and runs a backorder consolidation check (emits `BACKORDER_COVERABLE` if newly sufficient stock covers an open BackorderItem)

**Response shape for StockLevel:**
```json
{
  "id": "uuid",
  "warehouseId": "uuid",
  "productId": "uuid",
  "product": { "id": "uuid", "name": "string", "unit": "string" },
  "onHand": 100,
  "reserved": 20,
  "reorderPoint": 10,
  "reorderQty": 50
}
```

### Socket.io Events

| Event           | Direction     | Payload                                                                              | Description |
|-----------------|---------------|--------------------------------------------------------------------------------------|-------------|
| `STOCK_UPDATED` | Server→Client | `{ warehouseId, productId, onHand, reserved, available }` | Fires after every setStock or adjustStock. Pushed to role:ADMIN, role:SALES_MANAGER, role:FINANCE — Screen 7 stock table patches in-place without a refetch. |

---

## Fulfillment — `/api/fulfillment`

_Implementation: `src/modules/fulfillment/`_

> The auto-split algorithm (Global Constraint 3) is a pure function in `src/utils/computeFulfillmentSplit.js`. It prefers fewer warehouses (single-warehouse full-coverage first), then lowest `shippingCostWeight` to break ties. All commit paths re-validate live stock inside a Prisma transaction.

| Method | Path                                                | Auth | Roles                                    | Description |
|--------|-----------------------------------------------------|------|------------------------------------------|-------------|
| GET    | `/api/fulfillment`                                  | JWT  | ADMIN, SALES_MANAGER, FINANCE, SALES_REP | List orders with status in `[PENDING_FULFILLMENT, SPLIT_PENDING, PARTIALLY_FULFILLED, BACKORDERED]`, including committed splits and open backorders |
| GET    | `/api/fulfillment/:orderId`                         | JWT  | ADMIN, SALES_MANAGER, FINANCE, SALES_REP | Fulfillment detail for one order. Returns `suggestedSplit` (freshly computed), `committedSplits`, `openBackorders`, `order.lines` |
| POST   | `/api/fulfillment/:orderId/suggest-split`           | JWT  | ADMIN, SALES_MANAGER, FINANCE, SALES_REP | Preview auto-computed split against live stock — **no commit, no stock reservation** |
| POST   | `/api/fulfillment/:orderId/accept-split`            | JWT  | ADMIN, SALES_MANAGER, FINANCE            | Commit the auto-computed split. Decrements `onHand`/increments `reserved` per warehouse. Creates `BackorderItem` rows for any shortfall. Advances `Order.status`. |
| POST   | `/api/fulfillment/:orderId/override`                | JWT  | ADMIN, SALES_MANAGER, FINANCE            | Commit a manually supplied split. Same stock validation as auto path. Body: `{ splits: [{ warehouseId, productId, qty }] }` |
| POST   | `/api/fulfillment/backorders/:backorderId/consolidate` | JWT | ADMIN, SALES_MANAGER, FINANCE          | Resolve a backorder now that stock is available. Runs auto-split for the remaining qty, commits it, marks BackorderItem resolved. If partial, creates a new smaller backorder. |

**Route declaration note:** `/backorders/:backorderId/consolidate` is declared **before** `/:orderId` in the router so Express does not mistakenly treat the literal string `"backorders"` as an orderId.

**`GET /api/fulfillment/:orderId` response shape:**
```json
{
  "order": {
    "id": "uuid",
    "status": "PENDING_FULFILLMENT",
    "confirmedAt": "ISO-8601",
    "customer": { "id": "uuid", "companyName": "string" },
    "lines": [
      {
        "id": "uuid",
        "productId": "uuid",
        "product": { "id": "uuid", "name": "string", "unit": "string" },
        "quantity": 10,
        "unitPrice": "99.00",
        "discountPercent": "5.00",
        "lineType": "ONE_TIME"
      }
    ]
  },
  "suggestedSplit": [
    { "warehouseId": "uuid", "productId": "uuid", "qty": 10, "estimatedCost": 10.0 }
  ],
  "suggestedBackorders": [
    { "productId": "uuid", "qtyPending": 3 }
  ],
  "committedSplits": [
    {
      "id": "uuid",
      "warehouseId": "uuid",
      "warehouse": { "id": "uuid", "name": "string", "shippingCostWeight": "1.5" },
      "productId": "uuid",
      "product": { "id": "uuid", "name": "string", "unit": "string" },
      "qtyFulfilled": 10,
      "estimatedCost": "15.00"
    }
  ],
  "openBackorders": [
    {
      "id": "uuid",
      "productId": "uuid",
      "product": { "id": "uuid", "name": "string" },
      "qtyPending": 3,
      "resolvedAt": null
    }
  ]
}
```

**`POST /api/fulfillment/:orderId/override` body:**
```json
{
  "splits": [
    { "warehouseId": "uuid", "productId": "uuid", "qty": 8 },
    { "warehouseId": "uuid", "productId": "uuid", "qty": 2 }
  ]
}
```
- Total qty per productId across all split rows must not exceed the ordered quantity for that product
- Any shortfall auto-creates BackorderItem rows
- All warehouse+product combinations are live-validated against available stock inside the transaction

**Order status progression:**

| Status                | Meaning |
|-----------------------|---------|
| `PENDING_FULFILLMENT` | Order confirmed, split not yet run |
| `BACKORDERED`         | Split committed but ≥1 BackorderItem unresolved |
| `SPLIT_ACCEPTED`      | All lines split and reserved; no open backorders |

### Socket.io Events

| Event                  | Direction     | Payload                                                                                 | Description |
|------------------------|---------------|-----------------------------------------------------------------------------------------|-------------|
| `BACKORDER_COVERABLE`  | Server→Client | `{ orderId, backorderId, productId, qtyPending, totalAvailable }`                       | Fires after a stock change when total available stock now covers an open backorder. Pushed to role:ADMIN, role:SALES_MANAGER, role:FINANCE. Screen 8 uses this to auto-surface the "Consolidate Remaining Backorder" banner without a page refresh. |
| `FULFILLMENT_UPDATED`  | Server→Client | `{ orderId, status, splits: [...], backorders: [...] }`                                 | Fires after accept-split, override, or consolidate commit. Pushed to role:ADMIN, role:SALES_MANAGER, role:FINANCE, and `order:{orderId}`. Screen 7 order list and Screen 8 detail both update from this. |

---

## Track A → Track B Handoff Event

> **Coordination contract between Dev 1 (Quotations/Approvals) and Dev 2 (Fulfillment).**
>
> When a quotation is confirmed (either directly if no approval required, or after the final approval step clears), Track A must create an `Order` record and emit the event below so Track B's fulfillment flow can begin.

**Trigger:** `POST /api/quotations/:id/confirm` success (or final approval action that auto-confirms)

**Action Track A must perform:**
1. Create `Order` record: `{ quotationId, status: "PENDING_FULFILLMENT", confirmedAt: now() }`
2. Call `realtime.emitFulfillmentUpdated({ orderId, status: "PENDING_FULFILLMENT", splits: [], backorders: [] })`

**Socket.io event pushed:** `FULFILLMENT_UPDATED` (see Fulfillment section above)

**Payload shape Track A must emit:**
```json
{
  "orderId": "uuid",
  "status": "PENDING_FULFILLMENT",
  "splits": [],
  "backorders": []
}
```

Track B's fulfillment list query picks up new `PENDING_FULFILLMENT` orders from this event — no polling required.

---

## Subscriptions — `/api/subscriptions`

_Implementation: `src/modules/subscriptions/`_

| Method | Path                           | Auth | Roles                         | Description |
|--------|--------------------------------|------|-------------------------------|-------------|
| GET    | `/api/subscriptions`           | JWT  | ADMIN, FINANCE, SALES_MANAGER | List active subscriptions |
| GET    | `/api/subscriptions/:id`       | JWT  | ADMIN, FINANCE, SALES_MANAGER | Get subscription |
| POST   | `/api/subscriptions`           | JWT  | ADMIN                         | Create subscription record |
| PUT    | `/api/subscriptions/:id`       | JWT  | ADMIN, FINANCE                | Update (pause/cancel/resume) |
| DELETE | `/api/subscriptions/:id`       | JWT  | ADMIN                         | Delete |

---

## Billing — `/api/billing`

_Implementation: `src/modules/billing/`_

| Method | Path                    | Auth | Roles           | Description |
|--------|-------------------------|------|-----------------|-------------|
| GET    | `/api/billing`          | JWT  | ADMIN, FINANCE  | List billing records |
| GET    | `/api/billing/:id`      | JWT  | ADMIN, FINANCE  | Get billing record |
| POST   | `/api/billing`          | JWT  | ADMIN           | Create billing record |
| PUT    | `/api/billing/:id`      | JWT  | ADMIN, FINANCE  | Update billing record |
| DELETE | `/api/billing/:id`      | JWT  | ADMIN           | Delete billing record |

---

## Invoices — `/api/invoices`

_Implementation: `src/modules/invoices/`_

| Method | Path                      | Auth | Roles                         | Description |
|--------|---------------------------|------|-------------------------------|-------------|
| GET    | `/api/invoices`           | JWT  | ADMIN, FINANCE, SALES_MANAGER | List invoices |
| GET    | `/api/invoices/:id`       | JWT  | ADMIN, FINANCE, SALES_MANAGER, CUSTOMER | Get invoice (CUSTOMER scoped to own) |
| POST   | `/api/invoices`           | JWT  | ADMIN, FINANCE                | Create invoice |
| PUT    | `/api/invoices/:id`       | JWT  | ADMIN, FINANCE                | Update invoice |
| DELETE | `/api/invoices/:id`       | JWT  | ADMIN                         | Delete invoice |
| GET    | `/api/invoices/:id/pdf`   | JWT  | ADMIN, FINANCE, CUSTOMER      | Download invoice PDF |

---

## Payments — `/api/payments`

_Implementation: `src/modules/payments/`_

| Method | Path                             | Auth    | Roles                         | Description |
|--------|----------------------------------|---------|-------------------------------|-------------|
| GET    | `/api/payments`                  | JWT     | ADMIN, FINANCE                | List payments |
| GET    | `/api/payments/:id`              | JWT     | ADMIN, FINANCE, CUSTOMER      | Get payment |
| POST   | `/api/payments/create-order`     | JWT     | ADMIN, FINANCE, CUSTOMER      | Create Razorpay order for an invoice |
| POST   | `/api/payments/verify`           | PUBLIC  | —                             | Razorpay webhook — verify signature and mark invoice PAID |

---

## Portal — `/api/portal`

_Implementation: `src/modules/portal/`_

> All portal routes require `role = CUSTOMER` and enforce `customer:{customerId}` scope.

| Method | Path                             | Auth | Roles    | Description |
|--------|----------------------------------|------|----------|-------------|
| GET    | `/api/portal/quotations`         | JWT  | CUSTOMER | List own quotations |
| GET    | `/api/portal/quotations/:id`     | JWT  | CUSTOMER | View quotation detail |
| POST   | `/api/portal/quotations/:id/negotiate` | JWT | CUSTOMER | Send negotiation message |
| GET    | `/api/portal/invoices`           | JWT  | CUSTOMER | List own invoices |
| GET    | `/api/portal/invoices/:id`       | JWT  | CUSTOMER | View invoice |
| GET    | `/api/portal/invoices/:id/pdf`   | JWT  | CUSTOMER | Download invoice PDF |

---

## Deal Health — `/api/deal-health`

_Implementation: `src/modules/dealHealth/`_

| Method | Path                          | Auth | Roles                              | Description |
|--------|-------------------------------|------|------------------------------------|-------------|
| GET    | `/api/deal-health`            | JWT  | ADMIN, SALES_MANAGER, FINANCE      | List all health flags |
| GET    | `/api/deal-health/:id`        | JWT  | ADMIN, SALES_MANAGER, FINANCE      | Get single flag |
| POST   | `/api/deal-health`            | JWT  | ADMIN                              | Manually create flag |
| PUT    | `/api/deal-health/:id`        | JWT  | ADMIN, SALES_MANAGER               | Resolve or update flag |
| DELETE | `/api/deal-health/:id`        | JWT  | ADMIN                              | Delete flag |

### Socket.io Events

| Event                | Direction     | Payload                                     | Description |
|----------------------|---------------|---------------------------------------------|-------------|
| `DEAL_HEALTH_FLAGGED`| Server→Client | `{ id, quotationId, type, detail }`         | New flag detected — pushed to role:SALES_MANAGER, role:FINANCE, role:ADMIN |

---

## Reports — `/api/reports`

_Implementation: `src/modules/reports/`_

| Method | Path                    | Auth | Roles                         | Description |
|--------|-------------------------|------|-------------------------------|-------------|
| GET    | `/api/reports`          | JWT  | ADMIN, FINANCE, SALES_MANAGER | List available reports |
| GET    | `/api/reports/:id`      | JWT  | ADMIN, FINANCE, SALES_MANAGER | Get report data |
| POST   | `/api/reports`          | JWT  | ADMIN, FINANCE                | Generate / save report |
| PUT    | `/api/reports/:id`      | JWT  | ADMIN, FINANCE                | Update report |
| DELETE | `/api/reports/:id`      | JWT  | ADMIN                         | Delete report |

---

## Notifications — `/api/notifications`

_Implementation: `src/modules/notifications/`_

| Method | Path                              | Auth | Roles | Description |
|--------|-----------------------------------|------|-------|-------------|
| GET    | `/api/notifications`              | JWT  | ALL   | List own notifications |
| GET    | `/api/notifications/:id`          | JWT  | ALL   | Get single notification |
| PUT    | `/api/notifications/:id/read`     | JWT  | ALL   | Mark as read |
| DELETE | `/api/notifications/:id`          | JWT  | ALL   | Delete notification |

### Socket.io Events

| Event                   | Direction     | Payload                                      | Description |
|-------------------------|---------------|----------------------------------------------|-------------|
| `NOTIFICATION_CREATED`  | Server→Client | `{ id, type, message, relatedId }`           | New notification pushed to `user:{userId}` |

---

## Activity Logs — `/api/activity-logs`

_Implementation: `src/modules/activityLogs/`_

| Method | Path                       | Auth | Roles           | Description |
|--------|----------------------------|------|-----------------|-------------|
| GET    | `/api/activity-logs`       | JWT  | ADMIN, FINANCE  | List audit log entries (filterable by entity/user) |
| GET    | `/api/activity-logs/:id`   | JWT  | ADMIN, FINANCE  | Get single log entry |

> Activity logs are write-only via the internal `logAudit()` helper — no external POST endpoint.

---

## Socket.io — Room Reference

| Room                    | Who joins                   | Events received |
|-------------------------|-----------------------------|-----------------|
| `user:{userId}`         | Every authenticated user    | Personal notifications, approval actions, quotation updates |
| `role:{role}`           | Every authenticated user    | Role-wide broadcasts (e.g. approval queue, deal health flags, stock updates, fulfillment updates) |
| `customer:{customerId}` | CUSTOMER-role only          | Portal quotation/negotiation/invoice updates |
| `order:{orderId}`       | Not auto-joined — targeted by server when emitting fulfillment updates | `FULFILLMENT_UPDATED` — allows Track A quotation screens to react to order status changes without joining a role room |

---

<!-- Each module team appends their section below in this format:

## ModuleName — `/api/<route>`

| Method | Path | Auth | Roles | Description |
| ...    | ...  | ...  | ...   | ...         |

### Socket.io Events (if any)

| Event | Direction | Payload | Description |
| ...   | ...       | ...     | ...         |

-->
